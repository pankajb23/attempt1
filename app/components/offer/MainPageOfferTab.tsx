import { useStoreContext } from "app/lib/context/StoreContext";

export default function MainPageOfferTabs({}){
    const {modalsAndStoreId} = useStoreContext();
    
    return (
        <>

        </>
    );
}
